rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /executions/{document} {
      allow read, write: if true;
    }    
    match /cities/{document} {
      allow read, write: if request.auth != null && (request.auth.token.cli);
    }    
  }
}
